language: python
os: linux
dist: jammy
python:
  - "3.10"  # Specify the Python version you are using
install:
  - pip install -r requirements.txt  # Install dependencies
before_script:
  - python manage.py migrate
script:
  - python manage.py test  # Run your tests

